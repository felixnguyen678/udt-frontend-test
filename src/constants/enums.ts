export enum EOperator {
  ADDITION = '+',
  SUBTRACTION = '-',
  MULTIPLICATION = '*',
  DIVISION = '/'
}

export enum EEditMethod {
  CLEAR = 'clear',
  OPPOSITE = 'opposite',
  PERCENT = 'percent'
}

